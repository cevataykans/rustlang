// mod plant_structures;
mod single;

// use std::collections::{HashMap, HashSet};
// use std::fs::{self, File}; // import both `std::fs` and `std::fs::File
// use std::io::prelude::*; // import everything // import both

// use std::io::Result as IOResult; // Result is not have alias IOResult!

fn main() {
    println!("Hello, world!");

    println!("{}", crate::single::NUM);
}
